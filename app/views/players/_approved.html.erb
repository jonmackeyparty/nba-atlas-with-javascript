<div class='row'>
  <h4 class="eight columns">Active Leagues as Player</h4>
  <div class="four columns">
    <button type="button" id="leagues">View Leagues</button></br>
    <div id="player_leagues"></div>
    <%# Old code for iterating over approved invites
    <%# <% if @player.invitations.approved.any? %>
    <%#  <% @player.invitations.approved.uniq.each do |i| %>
    <%# <%= link_to "#{i.league.name}", league_path(i.league) %>
      <%#   <% end %>
    <%#<% else %>
        <%#<p>No active Leagues</p>
    <%# <% end %>
  </div>
</div>
